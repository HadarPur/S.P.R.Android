package com.example.hpur.spr.Logic.Queries;

// callback interface for FB
public interface CheckUserCallbacks {
    void checkUserFirebaseCallback(boolean result);
    void checkUserExistResetCallBack(boolean result);
}
